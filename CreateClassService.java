class CreateClassService{

 
  private boolean createClassService(String name, ZonedDateTime startDate, ZonedDateTime endDate, int duration, int maxCapacity){
    Class clubClass = new Class(name, startDate, endDate, duration, maxCapacity);
    
    
  }
  
}
